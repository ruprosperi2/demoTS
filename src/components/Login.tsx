
export const Login = () => {
  return (
    <form>
      <label htmlFor="email">Correo electrónico</label>
      <input id="email" placeholder="correo@correo.com" />

      <label htmlFor="password">Contraseña</label>
      <input type="password" id="password" placeholder="*****" />

    </form>
  );
};

